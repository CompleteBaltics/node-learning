<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>{{message}}</title>
  </head>
  <body>
    <div class="message">
      {{upper message}}
      {{message}}
      {{> header}}
    </div>
  </body>
</html>
